# web

SvelteKit frontend for rmc. See the [root README](../README.md) for setup, testing, and CI.

Scaffolded with:

```sh
pnpm dlx sv@0.16.3 create --template minimal --types ts --add prettier eslint vitest="usages:unit" tailwindcss="plugins:none" --no-download-check --install pnpm web
```
